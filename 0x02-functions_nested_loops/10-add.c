#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a, @b are two integers
 * 
 * Return: no return
 */
int add(int a, int b)
{
int c;
int d;
int res;
res = c+d;
return(res);
}
