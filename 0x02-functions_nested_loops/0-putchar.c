#include "main.h"
/**
 *
 * main - _putchar - writes the character c to stdout
 *Return: Always 0 (Success)
 */

int main(void)
{
int i;
char a[] = "_putchar";

for (i = 0; i < 9; i++)
}
_putchar(p[i]);
}
_putchar('\n');
return (0);
}
